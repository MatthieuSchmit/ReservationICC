{#  {% set url = path('ads_show', {'slug': ad.slug}) %} #}

<div class="col-md-2">

    <div class="card bg-light mb-3">


        <a href=" {{ path('show_getshow', {'slug':show.slug}) }} ">
            <img src="{{ show.coverImage }}" alt="Photo " width="100%"  class="img-thumbnail">
        </a>

        <div class="card-body">
            <h4 class="card-title">
                <a href=" {{ path('show_getshow', {'slug':show.slug}) }} ">
                {{ show.title }}
                </a>
            </h4>
            <p class="card-text"></p>

            <a href=" {{ path('show_getshow', {'slug':show.slug}) }} " class="btn btn-secondary">En savoir plus</a>

        </div>

    </div>

</div>
